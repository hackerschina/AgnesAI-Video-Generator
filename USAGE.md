# 📖 Agnes AI 智能创作助手 - 使用说明

## 🎯 目录

- [快速上手](#快速上手)
- [API 配置](#api-配置)
- [视频生成](#视频生成)
- [AI 对话](#ai-对话)
- [项目管理](#项目管理)
- [角色档案](#角色档案)
- [漫剧生成](#漫剧生成)
- [数据目录](#数据目录)
- [快捷键](#快捷键)
- [常见问题](#常见问题)

---

## 快速上手

### 首次使用流程

```
1. 启动程序 → 2. 配置 API Key → 3. 选择数据目录 → 4. 保存配置 → 5. 开始创作
```

### 基本工作流

1. **选择或创建项目**：在左侧项目列表管理你的创作项目
2. **配置角色档案**：为项目创建角色，确保角色一致性
3. **生成视频**：在视频生成页面输入描述和参数
4. **保存下载**：生成完成后保存视频到本地

---

## API 配置

### 获取 API Key

1. 访问 [Agnes AI 官网](https://agnes-ai.com/) 注册账号
2. 登录后进入 API 管理页面
3. 创建 API Key 并复制

### 配置步骤

1. 启动程序，在左侧"API 配置"区域输入：
   - **API Key**：你的 Agnes AI API Key
   - **API Base URL**：默认 `https://apihub.agnes-ai.com`
2. 点击"💾 保存配置"
3. 点击"🔌 测试连接"验证

### 支持的模型

- `agnes-2.0-flash`：对话模型
- `agnes-video-v2.0`：视频生成模型

---

## 视频生成

### 基本参数

| 参数 | 可选值 | 说明 |
|------|--------|------|
| 分辨率 | 720p (1280×720) / 1080p (1920×1080) | 视频分辨率 |
| 帧数 | 81 / 121 / 181 / 241 | 视频总帧数 |
| 帧率 | 24 / 30 | 每秒帧数 |

### 提示词技巧

1. **描述要具体**：
   ```
   一个年轻女性站在海边，金色长发随风飘动，夕阳西下，光线柔和
   ```

2. **指定风格**：
   ```
   动漫风格，手绘质感，明亮色彩
   ```

3. **包含角色信息**（使用角色档案时自动添加）：
   ```
   [角色描述] + [场景描述]
   ```

### 生成流程

```
输入提示词 → 设置参数 → 点击生成 → 等待完成 → 下载保存
```

### 视频下载

- 生成完成后，结果区域会显示视频链接
- 点击链接可在浏览器中打开
- 点击"💾 保存视频"可下载到本地

---

## AI 对话

### 使用场景

- 💡 **创意发散**：与 AI 讨论故事构思
- 📝 **剧本写作**：让 AI 帮助撰写剧本
- 🎭 **角色对话**：生成角色台词
- 🌍 **世界观构建**：构建故事世界观

### 操作说明

1. 切换到"💬 AI 对话"标签页
2. 在输入框输入消息
3. 按 `Enter` 发送，`Shift+Enter` 换行
4. 等待 AI 回复

### 对话示例

**你**：帮我写一个关于太空探险的故事大纲

**AI**：好的，以下是一个太空探险故事的大纲...

**你**：继续，主角的性格应该是什么样的？

**AI**：主角可以设定为...

---

## 项目管理

### 创建项目

1. 在左侧项目区域点击"➕ 新建项目"
2. 输入项目名称
3. 选择项目类型：
   - **视频项目**：普通视频生成项目
   - **漫剧项目**：漫剧/短剧生成项目
4. 点击"确定"

### 项目操作

- **选择项目**：点击项目列表中的项目
- **重命名项目**：右键项目 → 重命名
- **删除项目**：右键项目 → 删除（会同时删除所有数据）
- **导出项目**：右键项目 → 导出（JSON 格式）

### 项目数据

每个项目包含：
- 角色档案列表
- 场景列表
- 已生成视频列表
- 创作笔记

---

## 角色档案

### 创建角色

1. 选中一个项目
2. 在角色档案区域点击"➕ 添加角色"
3. 填写角色信息：
   - **名称**：角色姓名
   - **性别**：女 / 男 / 其他
   - **年龄**：1-100
   - **外貌**：发型、发色、眼睛、肤色、身高等
   - **服装**：风格、颜色、配饰等
   - **性格**：性格特点、说话风格
   - **背景**：角色故事、身份

### 编辑角色

- 双击角色档案进行编辑
- 点击"保存"更新

### 删除角色

- 点击角色旁边的"删除"按钮

### 角色在视频生成中的作用

角色档案的描述会自动拼接并添加到视频生成的提示词中。例如：

```
角色描述: 小红(女, 25岁), 外貌: 黑色长发, 大眼睛, 皮肤白皙, 服装: 白色连衣裙, 性格: 温柔善良, 背景: 设计师
场景描述: 小红坐在窗边，阳光透过窗帘洒进来
```

这确保了同一角色在不同场景中保持外观和性格的一致性。

---

## 漫剧生成

### 流程概览

```
输入故事 → AI 生成分镜 → 编辑场景 → 批量生成
```

### 详细步骤

#### 1. 创建漫剧项目
- 新建项目时选择"漫剧项目"类型

#### 2. 输入故事描述
- 切换到"🖼️ 漫剧生成"标签页
- 在"故事描述"中输入你的故事创意

#### 3. 生成分镜剧本
- 点击"📝 生成分镜剧本"
- AI 会自动将故事拆解为多个分镜
- 每个分镜包含：场景描述、角色动作、镜头语言

#### 4. 编辑场景
- 查看生成的分镜列表
- 点击场景进行编辑
- 可修改每个场景的描述
- 可添加/删除场景

#### 5. 批量生成视频
- 点击"🎬 批量生成所有场景"
- 系统会依次为每个场景生成视频
- 可查看每个场景的生成进度
- 生成完成后可逐个预览和保存

### 场景描述优化建议

1. **具体描述**：
   ```
   小红站在雨中，表情悲伤，缓缓抬起头
   ```

2. **指定镜头**：
   ```
   特写镜头：小红的眼睛里映着雨滴
   ```

3. **添加氛围**：
   ```
   冷色调，慢镜头，雨水打湿头发
   ```

---

## 数据目录

### 默认位置

```
C:\Users\<用户名>\AgnesAI_Data\
```

### 目录结构

```
AgnesAI_Data/
├── config.json              # 全局配置
├── projects/                # 项目数据
│   ├── {project_id}.json    # 每个项目一个文件
│   └── ...
└── videos/                  # 下载的视频
    ├── {video_id}.mp4
    └── ...
```

### 切换数据目录

1. 点击"📁 选择目录"
2. 选择新的目录（可选择已有目录，或新建目录）
3. 确认后切换
4. 所有数据会保存到新目录

### 迁移数据

将旧目录的内容复制到新目录即可：
```
复制旧目录的所有文件 → 粘贴到新目录 → 重启程序
```

### 备份

定期备份整个数据目录：
```
复制 AgnesAI_Data 文件夹 → 粘贴到备份位置
```

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息（AI 对话中） |
| `Shift+Enter` | 换行（AI 对话中） |
| `Ctrl+S` | 保存配置 |
| `Ctrl+Enter` | 生成视频 |

---

## 常见问题

### Q: 如何更换 API Key？
在左侧"API 配置"区域直接修改，然后点击"💾 保存配置"。

### Q: 如何保存当前所有设置？
点击"💾 保存配置"，程序会保存：
- API Key
- Base URL
- 窗口位置和大小
- 数据目录路径

### Q: 配置文件在哪里？
在数据目录下的 `config.json` 文件中。

### Q: 如何在不同电脑间同步数据？
将整个数据目录复制到新电脑，然后在程序中选择该目录即可。

### Q: 视频生成失败怎么办？
1. 检查网络连接
2. 确认 API Key 是否有效
3. 查看错误日志 (`error.log`)
4. 重试或换一个提示词

### Q: 如何提高视频生成质量？
- 使用更具体的提示词
- 添加角色档案确保一致性
- 适当提高分辨率和帧数

### Q: 程序支持哪些操作系统？
- Windows 10/11（主要支持）
- macOS 10.14+
- Linux（Ubuntu 18.04+）

### Q: 打包的 exe 文件如何分发？
1. 打开 `dist` 文件夹
2. 将 `AgnesAI-Video-Generator.exe` 发送给他人
3. 对方双击即可使用（首次启动较慢，因为要解压）

---

## 更新日志

### v1.0.0
- 初始版本发布
- 支持 AI 视频生成
- 支持 AI 对话
- 支持角色档案管理
- 支持漫剧生成
- 支持数据持久化
